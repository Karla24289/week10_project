### Task: Core Chat Application
**Prompt:** "Task 1: Core Chat Application (100 points). Build the app through Parts A-D with wide page setup, safe Hugging Face API connection, native Streamlit multi-turn chat UI, sidebar chat management, and JSON chat persistence in chats/."
**AI Suggestion:** Replace the starter single-message prototype with a full Streamlit chat app that uses `st.set_page_config`, loads `HF_TOKEN` from `st.secrets`, handles missing or invalid tokens safely, uses `st.chat_message` and `st.chat_input` for conversation UI, stores chat history in `st.session_state`, manages multiple chats from the sidebar, and saves each chat as a separate JSON file in `chats/`.
**My Modifications & Reflections:** The generated structure fit the assignment well. I kept the Hugging Face token out of `app.py`, added persistent chat files, and used Streamlit's native chat components instead of custom CSS bubbles. I would still review the interface and behavior against the assignment screenshot and make any small wording or layout tweaks my instructor expects.

### Task: Response Streaming
**Prompt:** "Task 2: Response Streaming (20 points). Use `stream=True` with the Hugging Face chat completions API, render the reply incrementally in Streamlit, and save the full streamed response to chat history when complete."
**AI Suggestion:** Update the API request to use server-sent event streaming, parse each `data:` chunk from the response, render text incrementally in the assistant chat area with native Streamlit streaming support, and store the complete assembled assistant response in the saved chat history after the stream finishes.
**My Modifications & Reflections:** The streaming version fit the existing chat structure without changing the rest of the assignment features. I added a very small delay so the token-by-token effect is visible even if the model streams quickly. I would still test with a few prompts in the browser to confirm the streaming effect is noticeable enough for grading.

### Task: User Memory
**Prompt:** "Task 3: User Memory (20 points). After each assistant response, make a second API call to extract user traits/preferences, store them in memory.json, show them in a sidebar expander, add a control to clear memory, and inject stored memory into future prompts for personalization."
**AI Suggestion:** Add persistent memory helpers around `memory.json`, use a second lightweight extraction prompt that returns JSON, merge any extracted traits into saved memory, display that memory in the sidebar with a clear/reset button, and prepend a system prompt built from the saved memory to future chat requests.
**My Modifications & Reflections:** This approach kept the memory feature separate from the main chat flow, which made it easier to add without breaking the earlier tasks. I chose a flexible JSON structure so different kinds of traits can be stored as they appear. I would still test with a few personal facts to make sure the extraction is accurate enough and the personalization shows up clearly in later replies.
